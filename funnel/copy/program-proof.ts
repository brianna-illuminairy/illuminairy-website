import { satProgram } from "@/lib/site";

export const programProofBullets = [
  `Georgia Tech mentors — 1450+ on the SAT`,
  `${satProgram.maxPerClass} students per class · ${satProgram.privateSessions} private 1:1 sessions included`,
  `Progress report to parents every week`,
  `${satProgram.tuitionDisplay} published · ${satProgram.weeks}-week program`
] as const;
