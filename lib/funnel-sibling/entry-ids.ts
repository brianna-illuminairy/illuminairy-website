/** Stable DOM ids for SSR entry shells — client runner removes on mount. */
export const FUNNEL_ENTRY_SHELL_IDS = {
  planA: "plan-a-entry-ssr",
  planB: "plan-b-entry-ssr",
  scoreReview: "score-review-entry-ssr",
} as const;
